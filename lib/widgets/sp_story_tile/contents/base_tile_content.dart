part of sp_story_tile;

abstract class _BaseTileContent extends StatelessWidget {
  const _BaseTileContent({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
